# Introduction

For the planbot module, I'm planning to add more proposals to be generated by this bot.  Those proposal will be added over the time and more or less independent of each other so needs separate configuration.  I divide the whole process into different sprint

# Sprint 1
 The first step is to isolate the planbot configuration into its own so that we can easily add new configuration.  config/config_planbot.yaml
 
 With this new configuration.  The original planbot file path are moved so that more proposal specific data can be accommodated and isolated with each other.

 Change the code and run configuration to reflect the new config yaml setup.
 
 # Sprint 2
 Review the current crewai prompt mechanism (agent.yaml and tasks.yaml).  Realign our prompt structure to it when necessary.

 # Sprint 3
 Add the required new report 